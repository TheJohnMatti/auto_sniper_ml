# Copy to .env and fill in. Only the provider selected in config.yaml
# (ml_pipeline.llm_labeling.provider) is required; the pipeline falls back to a
# heuristic labeler when no key is present.

OPENAI_API_KEY=
GOOGLE_API_KEY=
